@model IEnumerable<WebApplication1.Models.Entities.Assignment >
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div class="col-md-5">
    @foreach (var item in Model)
    {
    <a href="">@Html.DisplayFor(modelItem => item.Title)</a>
    <br />
    }
</div>

